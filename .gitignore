# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
# ...but the built frontend bundle must ship in the deployment image.
# `langgraph deploy` filters its upload with THIS file only (nested
# .gitignore files are not read), so the negation has to live here.
!frontend/dist/
*.egg-info

# Virtual environments
.venv

.env
.env.*
!.env.example
*.pem
*.key
*.crt
credentials.json
node_modules/
.DS_Store

# LangGraph CLI runtime state
.langgraph_api/
